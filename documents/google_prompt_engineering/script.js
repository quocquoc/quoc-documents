// JavaScript riêng cho tài liệu 1 (nếu cần)
console.log("Script cho Tài liệu 1 đã được tải.");

// Ví dụ: Thêm tương tác cho code block
document.querySelectorAll('pre code').forEach((block) => {
  // Thêm nút copy hoặc các tính năng khác
});